name: WildFly EE10 Maintenance

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
  # Run at 5:32 every day
  schedule:
     - cron: "32 5 * * *"

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  provision-matrix:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: [40.0.1.Final, 41.0.0.Final]
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Cache the installation
      - name: Cache WildFly Installation
        id: wildfly-cache
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/wildfly
          key: wildfly-${{ matrix.version}}-EE10

      # Cache the installation
      - name: Cache Galleon Installation
        id: galleon-installation
        uses: actions/cache/restore@v4
        with:
          path: ${{ github.workspace }}/galleon
          key: galleon-${{ vars.GALLEON_VERSION }}
          fail-on-cache-miss: true

      # If the WildFly Cache was a miss - provision the server.
      - name: Provision WildFly EE10
        if: steps.wildfly-cache.outputs.cache-hit != 'true'
        run: |
          cat > provisioning.xml << 'EOF'
          <installation xmlns="urn:jboss:galleon:provisioning:3.0">
              <feature-pack location="org.wildfly:wildfly-ee-10-feature-pack:${{ matrix.version }}"/>
              <feature-pack location="org.wildfly:wildfly-galleon-pack:${{ matrix.version }}"/>
          </installation>
          EOF
          ./galleon/bin/galleon.sh provision provisioning.xml --dir=wildfly

      # Upload the log files
      - name: Upload Log File
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: log-files
          path: ~/work/**/*.log
