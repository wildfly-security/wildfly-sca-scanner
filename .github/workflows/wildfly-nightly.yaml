name: WildFly Nightly Maintenance

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
  # Run at 5:47 every day
  schedule:
     - cron: "47 5 * * *"

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  provision-matrix:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        version: [upstream, maintenance]
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Cache the instllation
      - name: Cache Galleon Installation
        id: galleon-installation
        uses: actions/cache/restore@v4
        with:
          path: ${{ github.workspace }}/galleon
          key: galleon-${{ vars.GALLEON_VERSION }}
          fail-on-cache-miss: true

      # The matrix runs a set of steps for main and a set of steps for maintenance, internally
      # we map to the job name as seen in ci.wildfly.org

      - name: Map version to nightly name in CI
        if: ${{ matrix.version != 'maintenance' || vars.MAINTENANCE_CI != 'OFF' }}
        id: version-map
        run: |
          VERSION_NAME=${{ matrix.version}}

          if [[ $VERSION_NAME == "upstream" ]]
          then
            echo "CI_JOB=${{ vars.UPSTREAM_CI }}" >> $GITHUB_OUTPUT
          elif [[ $VERSION_NAME == "maintenance" ]]
          then
            echo "CI_JOB=${{ vars.MAINTENANCE_CI }}" >> $GITHUB_OUTPUT
          fi

      - name: Download Nightly Build
        if: ${{ matrix.version != 'maintenance' || vars.MAINTENANCE_CI != 'OFF' }}
        id: wildfly-nightly
        uses: wildfly-extras/wildfly-nightly-download@v1
        with:
          uri: https://ci.wildfly.org/guestAuth/repository/download/${{ steps.version-map.outputs.CI_JOB }}/latest.lastSuccessful/wildfly-maven-repository.tar.gz

      - name: Provision WildFly
        if: ${{ matrix.version != 'maintenance' || vars.MAINTENANCE_CI != 'OFF' }}
        run: " ./galleon/bin/galleon.sh install wildfly#${{ steps.wildfly-nightly.outputs.wildfly-version }} --dir=wildfly"

      # Cache the installation
      - name: Cache WildFly Installation
        if: ${{ matrix.version != 'maintenance' || vars.MAINTENANCE_CI != 'OFF' }}
        id: wildfly-cache
        uses: actions/cache/save@v4
        with:
          path: ${{ github.workspace }}/wildfly
          key: wildfly-standard-${{ matrix.version}}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Provision WildFly EE10
        if: ${{ matrix.version == 'maintenance' && vars.MAINTENANCE_CI != 'OFF' }}
        run: |
          rm -rf -- "${{ github.workspace }}/wildfly"
          cat > provisioning.xml << 'EOF'
          <installation xmlns="urn:jboss:galleon:provisioning:3.0">
              <feature-pack location="org.wildfly:wildfly-ee-10-feature-pack:${{ steps.wildfly-nightly.outputs.wildfly-version }}"/>
              <feature-pack location="org.wildfly:wildfly-galleon-pack:${{ steps.wildfly-nightly.outputs.wildfly-version }}"/>
          </installation>
          EOF
          ./galleon/bin/galleon.sh provision provisioning.xml --dir=wildfly

      # Cache the installation
      - name: Cache WildFly EE10 Installation
        if: ${{ matrix.version == 'maintenance' && vars.MAINTENANCE_CI != 'OFF' }}
        id: wildfly-ee10-cache
        uses: actions/cache/save@v4
        with:
          path: ${{ github.workspace }}/wildfly
          key: wildfly-ee10-${{ matrix.version}}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Provision WildFly Preview
        if: ${{ matrix.version != 'maintenance' || vars.MAINTENANCE_CI != 'OFF' }}
        run: |
          rm -fR ${{ github.workspace }}/wildfly
          ./galleon/bin/galleon.sh install wildfly-preview#${{ steps.wildfly-nightly.outputs.wildfly-version }} --dir=wildfly

      # Cache the installation
      - name: Cache WildFly Preview Installation
        if: ${{ matrix.version != 'maintenance' || vars.MAINTENANCE_CI != 'OFF' }}
        id: wildfly-preview-cache
        uses: actions/cache/save@v4
        with:
          path: ${{ github.workspace }}/wildfly
          key: wildfly-preview-${{ matrix.version}}-${{ github.run_id }}-${{ github.run_attempt }}

      # Upload the log files
      - name: Upload Log File
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: log-files
          path: ~/work/**/*.log
